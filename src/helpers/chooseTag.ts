// @ts-nocheck
export const chooseTag = (event) => {
    event.target.classList.toggle('selected');
    { id === selectedTag ? ' selected tag' : 'tag' }
    { (event) => { event.target.id === selectedTag ? 'tag selected' : 'tag'; } }
};
